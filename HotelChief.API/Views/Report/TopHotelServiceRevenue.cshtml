@model IEnumerable<HotelChief.Core.DTO.ProfitableHotelServiceReportDto>
<h2>Top Hotel Service Revenue Report</h2>
<form asp-action="GenerateTopHotelServiceRevenueReport" method="post">
    <label for="startDate">Start Date:</label>
    <input type="date" name="startDate" required />
    <label for="endDate">End Date:</label>
    <input type="date" name="endDate" required />
    <label for="topN">Top N:</label>
    <input type="number" name="topN" required />
    <button type="submit">Generate Report</button>
</form>

@if (@Model != null)
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.HotelServiceId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TotalOrders)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TotalRevenue)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.HotelServiceId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TotalOrders)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TotalRevenue)
                    </td>
                </tr>
            }
        </tbody>
    </table>
}